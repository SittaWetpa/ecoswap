// Placeholder entry point.
//
// The Cloud Functions environment (TypeScript scaffold, ESLint, secret wiring,
// and the three function stubs `issueQRToken`, `validateQRToken`, and
// `onTradeComplete`) is implemented in WBS 3.5. This file exists only so that
// `tsc --build` produces a `lib/` output during the WBS 3.4 task, which adds
// Cloud Storage security rules and the rules-test harness.
export {};

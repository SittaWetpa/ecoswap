{
  "name": "ecoswap-functions",
  "version": "0.1.0",
  "private": true,
  "description": "EcoSwap Cloud Functions and security rules tests.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "lint": "echo \"lint placeholder — configured in WBS 3.5\"",
    "test": "firebase emulators:exec --only storage --project demo-ecoswap \"jest --runInBand\"",
    "test:rules": "firebase emulators:exec --only storage --project demo-ecoswap \"jest --runInBand\"",
    "test:no-emulator": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.30",
    "firebase": "^10.12.2",
    "firebase-admin": "^12.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}

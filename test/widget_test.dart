// Smoke test replaced by per-screen widget tests in test/screens/.
// This file is intentionally minimal so the default test runner has
// at least one file to discover.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — real tests live in test/screens/ and test/services/', () {
    expect(true, isTrue);
  });
}
